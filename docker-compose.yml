version: '3.8'

services:
  agent-bot:
    build: .
    env_file:
      - .env
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./projects:/app/projects
      - ./mcp_config.json:/app/mcp_config.json
    network_mode: "host"
